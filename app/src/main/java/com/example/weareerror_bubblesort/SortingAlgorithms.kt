package com.example.weareerror_bubblesort

class SortingAlgorithms {

    fun BubbleSort(userArray: IntArray) {

        //first for loop that loops through the user array

        //second for loop for bubble sort
    }
}